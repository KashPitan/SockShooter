//constants to be shared with the server and client
//note this does not work delete later if not fixed

const consts = {
    CANVAS_SIZE: {
        x:400,
        y:400
    }
};

// export let clientConsts = consts;
module.exports = Object.freeze(consts);

